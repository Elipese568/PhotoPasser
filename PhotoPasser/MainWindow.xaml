<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PhotoPasser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoPasser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PhotoPasser">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>
    <Grid Padding="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<StackPanel x:Name="TitlebarArea" 
                Orientation="Horizontal" 
                Height="32" 
                HorizontalAlignment="Stretch" 
                
                Grid.Row="0" 
                Padding="12,0,0,0" 
                Spacing="12">
            <TextBlock Style="{ThemeResource CaptionTextBlockStyle}" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Center">Photo Passer</TextBlock>
        </StackPanel>-->
        <TitleBar x:Name="TitlebarArea" Grid.Row="0" Title="Photo Passer"/>
        <Frame x:Name="contentFrame" CacheSize="4" SourcePageType="local:TaskOverview" Grid.Row="1"/>
        
    </Grid>
</Window>
