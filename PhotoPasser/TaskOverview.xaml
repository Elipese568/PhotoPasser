<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PhotoPasser.TaskOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoPasser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:PhotoPasser.Converters"
    xmlns:helper="using:PhotoPasser.Helper"
    mc:Ignorable="d">
    <Page.Resources>
        <conv:UriImageSourceConverter x:Key="UriImageSourceConverter"/>
        <helper:SelfReference Value="{x:Bind}" x:Key="PageSelf"/>
        
    </Page.Resources>
    <Grid RowSpacing="36" Padding="36,40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock x:Uid="ms-resource:///TaskOverview/HomeLabel" Grid.Row="0" Style="{ThemeResource TitleTextBlockStyle}"/>
        <Grid Grid.Row="1" RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <GridView Name="TasksView" ItemsSource="{x:Bind ViewModel.Tasks}" Grid.Row="0" SelectionMode="None">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="local:FiltTask">
                        <UserControl x:Name="TaskItemPresenter" PointerEntered="LibraryItem_PointerEntered" PointerExited="LibraryItem_PointerExited" IsDoubleTapEnabled="True" DoubleTapped="TaskItemPresenter_DoubleTapped">
                            <Grid BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                  BorderThickness="1"
                                  Background="{ThemeResource CardBackgroundFillColorDefault}"
                                  ColumnSpacing="16"
                                  Height="200"
                                  Width="300"
                                  CornerRadius="{StaticResource OverlayCornerRadius}">
                                <Image Source="{x:Bind PresentPhoto,Converter={StaticResource UriImageSourceConverter}}" Stretch="UniformToFill"/>
                                <!--<Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                    <Rectangle.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <LinearGradientBrush.GradientStops>
                                                <GradientStop Color="{ThemeResource CardBackgroundFillColorSecondaryTransparent}" Offset="0.0"/>
                                                <GradientStop Color="{ThemeResource CardBackgroundFillColorSecondaryTransparent}" Offset="0.2"/>
                                                <GradientStop Color="{ThemeResource CardBackgroundFillColorSecondarySoild}" Offset="0.623"/>
                                                <GradientStop Color="{ThemeResource CardBackgroundFillColorSecondarySoild}" Offset="1"/>
                                            </LinearGradientBrush.GradientStops>
                                        </LinearGradientBrush>
                                    </Rectangle.Fill>
                                </Rectangle>-->
								<Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Fill="{x:Bind MaskBrush,Mode=OneWay}">
								</Rectangle>
								<Grid HorizontalAlignment="Stretch" MaxHeight="100" VerticalAlignment="Bottom" Padding="16,0,16,16">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="20" />
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Style="{ThemeResource BodyStrongTextBlockStyle}" Text="{x:Bind Name}"/>
                                    <TextBlock Grid.Row="1" Style="{ThemeResource BodyTextBlockStyle}" TextWrapping="Wrap" VerticalAlignment="Stretch" Text="{x:Bind Description}"/>
                                </Grid>
                                <Button Name="OptionSettingButton" Visibility="Collapsed" HorizontalAlignment="Right" Style="{StaticResource DefaultButtonStyle}" VerticalAlignment="Top" Margin="8">
                                    <FontIcon Glyph="&#xE712;" />
                                    <Button.Flyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem x:Uid="ms-resource:///TaskOverview/EditInformationFlyoutItem" Command="{Binding Value.ViewModel.EditTaskCommand, Source={StaticResource PageSelf}}" CommandParameter="{x:Bind}"/>
                                            <MenuFlyoutItem x:Uid="ms-resource:///TaskOverview/DeleteFlyoutItem" Command="{Binding Value.ViewModel.DeleteTaskCommand, Source={StaticResource PageSelf}}" CommandParameter="{x:Bind}"/>
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="OptionSettingButtonVisibility">
                                        <VisualState x:Name="OptionSettingButtonVisible">
                                            <VisualState.Setters>
                                                <Setter Target="OptionSettingButton.Visibility" Value="Visible"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="OptionSettingButtonDefault">
                                            <VisualState.Setters>
                                                <Setter Target="OptionSettingButton.Visibility" Value="Collapsed"/>
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </UserControl>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
            <Grid Grid.Row="1">
                <Grid x:Name="ShadowCastGrid"/>
                <Border x:Name="ShadowRect" HorizontalAlignment="Right" Translation="0,0,32" Loaded="ShadowRect_Loaded" Width="60" Height="60" CornerRadius="30">
                    <Border.Shadow>
                        <ThemeShadow x:Name="shadow"/>
                    </Border.Shadow>
                    <Button x:Name="AddTaskButton" Command="{x:Bind ViewModel.AddTaskCommand}" CornerRadius="30" Height="60" Width="60">
                        <FontIcon Glyph="&#xE710;" FontSize="30"/>
                    </Button>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
